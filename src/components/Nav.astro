<div class="nav container">
	<slot />
</div>

<style is:global>
	.nav {
		display: flex;
		flex-wrap: wrap;
		justify-content: center;
		padding: 0.4rem 1rem;
		gap: 1.3rem;
		text-align: center;
	}
	
	.nav a[href] {
		color: var(--text-color);
		text-decoration: none;
	}
</style>
