---
import { Constant } from "../constant";
import Layout from "./Layout.astro";

const { frontmatter } = Astro.props;
---

<Layout title={`${frontmatter.title} - ${Constant.brand}`} description={frontmatter.description ?? ""}>
    <slot />
</Layout>